{
  "name": "candyshare",
  "version": "1.0.0",
  "description": "A simple file-sharing platform that lets you upload files and generate secure temporary links for sharing. Perfect for quickly sharing files without the need for accounts or complex setup.",
  "private": "true",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && bun index.ts",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && bun run build",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "cd backend && bun install && cd ../frontend && npm install",
    "prepare": "is-ci || husky"
  },
  "keywords": [],
  "workspaces": [
    "frontend",
    "backend"
  ],
  "author": "anonymous961",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "is-ci": "^4.1.0",
    "lint-staged": "^16.1.6"
  }
}